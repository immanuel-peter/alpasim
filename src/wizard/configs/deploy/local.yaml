# @package _global_
# Deploy target: local workstation. Use with a topology config, e.g.:
#   uv run alpasim_wizard deploy=local topology=1gpu driver=vavam wizard.log_dir=./out

defines:
  filesystem: ${repo-relative:"data"}
